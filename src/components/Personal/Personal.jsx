import React from 'react';

import { SectionWrapper } from 'styles/common';
import { bgColor3 } from 'styles/theme';

const Personal = () => {
  return (
    <SectionWrapper className="personal" bgColor={bgColor3}>
      Personal
    </SectionWrapper>
  );
}

export default Personal;
