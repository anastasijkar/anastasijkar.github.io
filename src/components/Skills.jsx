import React from 'react';

import { SectionWrapper } from 'styles/common';
import { bgColor4 } from 'styles/theme';

const Skills = () => {
  return (
    <SectionWrapper className="skills" bgColor={bgColor4}>
      Skills
    </SectionWrapper>
  );
}

export default Skills;
