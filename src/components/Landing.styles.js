import styled from 'styled-components';

export const Portrait = styled.img`
  width: 40%;
  max-width: 120px;
  border-radius: 50%;
  margin: 2em;
  border: 1px inset;
  filter: grayscale(.8) contrast(1.1);
`;