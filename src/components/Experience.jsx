import React from 'react';

import { SectionWrapper } from 'styles/common';
import { bgColor2 } from 'styles/theme';

const Experience = () => {
  return (
    <SectionWrapper className="experience" bgColor={bgColor2}>
      Experience
    </SectionWrapper>
  );
}

export default Experience;
